# 实验报告（综合）

> 说明：本文件基于《小组提交文档-实验报告模板》要求，结合当前“文件备份软件”项目的代码仓库内容撰写。对于模板中信息不足或需根据实际情况补充的部分，均以“【待补充】”显式标注。

## 一、实验室名称

- 【待补充：例如“清水河校区××楼××实验室”或课程统一规定的实验室名称。】

## 二、实验项目名称

- 文件备份软件

## 三、实验目的

- 通过完整实现一款跨平台的文件备份软件，综合运用《程序设计（C/C++）》《数据结构与算法》《软件工程》等课程知识。
- 训练学生从需求分析、系统设计、编码实现、软件测试到部署交付的完整软件生命周期实践能力。
- 掌握 Qt/C++ 跨平台 GUI 开发、CMake 构建、Git 协作等现代软件工程工具链。
- 通过实现备份/恢复/验证、定时备份、实时备份等功能，提升对文件系统、元数据、特殊文件处理等系统级问题的理解。
- 通过团队协作完成项目，提升沟通协调、任务拆分、进度管理、代码评审等工程实践能力。

## 四、实验内容

- 设计并实现一款数据备份软件，以项目组形式推进，每组最多三人。
- 基于软件工程方法学推进项目，经历：
  - 需求分析：形成《需求分析说明书》；
  - 系统设计：形成《系统设计文档》；
  - 编码实现：基于 Qt/C++ 实现备份软件；
  - 软件测试：形成《软件测试报告》，并在仓库中加入自动化测试。
- 目标功能（对应模板中的基本要求 + 部分扩展要求）：
  - 数据备份：将目录树中的文件数据保存到指定位置（支持目录/单文件备份）。
  - 数据还原：将目录树中的文件数据恢复到指定位置。
  - 备份验证：通过哈希比对确保备份数据正确性。
  - 文件类型支持：支持普通文件、目录、符号链接，以及 UNIX 下的 FIFO、块/字符设备等。
  - 元数据支持：尽可能保留权限、属主/属组、访问/修改时间等元数据。
  - 自定义备份：支持打包为单文件、选择是否压缩/加密、是否包含特殊文件、是否验证等。
  - 压缩解压：支持 Huffman、RLE、Zlib 三种无损压缩算法。
  - 打包解包：支持生成单一 `.fbk` 备份包格式。
  - 加密备份：支持 XOR、RC4、“简化 AES-256” 三类加密方式（教学用途）。
  - 实时备份：通过 `QFileSystemWatcher` 监控目录变化并自动备份。
  - 定时备份：支持每天/每周/每月定时备份及备份保留策略。
  - 图形界面：基于 Qt Widgets 实现四个标签页（备份 / 恢复 / 实时备份 / 定时备份）。

## 五、实验器材（设备、元器件）

- PC 若干，支持以下环境之一：
  - Windows 10/11；
  - macOS（当前开发环境），已验证；
  - 常见 Linux 发行版（Ubuntu 等，需进一步验证）。
- 软件环境：
  - CMake ≥ 3.16；
  - Qt 6.x（Core/Gui/Widgets/Concurrent 模块）；
  - C++17 编译器（clang 或 MSVC / g++）；
  - Git；GitHub 远程仓库；
  - GoogleTest（通过 CMake FetchContent 在测试阶段拉取）；
  - 建议使用 VS Code 或 Qt Creator 作为 IDE。

## 六、实验步骤及操作（概览）

> 需求、设计、测试的详细内容在后续三个专门文档中展开，本节只给出整体流程。

1. 需求分析
   - 学习实验指导书和评分标准；
   - 讨论并确定目标功能、可选扩展功能；
   - 编写《需求分析说明书》草稿并评审；
   - 在 README 和 Homework 中记录。
2. 系统设计
   - 设计总体架构（UI 层 / 备份引擎 / 数据结构 / 测试层）；
   - 设计主要类与模块接口（`BackupEngine`、`BackupTypes`、`MainWindow` 等）；
   - 设计备份格式与 manifest JSON 结构；
   - 设计压缩/加密算法策略及扩展点；
   - 编写《系统设计文档》。
3. 编码实现
   - 按模块划分任务：引擎、压缩/加密、GUI、定时/实时备份；
   - 迭代开发：先实现基础功能，再逐步增加扩展功能；
   - 使用 Git 分支（如 `maindev`）协同开发，按功能提交；
   - 关键里程碑参考 Git 提交记录（见“需求分析说明书 / 5. 项目规划”）。
4. 软件测试
   - 设计测试用例，覆盖功能/边界/异常场景；
   - 使用 GoogleTest 编写单元测试与集成测试（见 `tests/` 目录）；
   - 手工测试 GUI 操作流程；
   - 记录测试结果与发现的问题；
   - 编写《软件测试报告》。
5. 文档与总结
   - 依据模板填写实验报告各部分；
   - 在本仓库中维护 `README.md`、`用户使用指南.md` 与本 `Homework.md`；
   - 完成总结与改进建议。

## 七、实验结论

- 本项目实现了一款基本可用且功能较丰富的文件备份软件，功能覆盖课程大部分基础与扩展要求。
- 技术上完成了：
  - Qt/C++ 跨平台 GUI；
  - 多种压缩算法（Huffman/RLE/Zlib）与加密算法（XOR/RC4/简化 AES）；
  - Manifest 驱动的备份格式；支持目录模式和 `.fbk` 包模式；
  - 定时备份和实时备份；备份保留策略；
  - 自动化单元测试与集成测试框架。
- 工程实践方面：
  - 使用 Git/GitHub 进行分支管理和 Pull Request 合并；
  - 通过 README 与用户指南提升文档质量；
  - 为后续扩展（如网络备份、增量备份）预留接口。
- 仍存在的不足和改进方向见后文“系统设计文档 / 8. 部署与扩展设计”和“软件测试报告 / 7. 测试结果分析与改进建议”。

## 八、总结及心得体会（示例文本，需按小组实际情况补充）

- 遇到的主要问题与解决思路：
  - 如何准确遍历目录并处理特殊文件：通过 `std::filesystem` 与 POSIX `stat`/`lstat`，同时设计 `FileKind` 枚举统一抽象。
  - 如何设计兼顾可扩展性的 manifest：采用 JSON 格式 + 版本字段 + 压缩/加密方法枚举，并在解析时兼容旧版本字段缺失。
  - UI 与后台任务的解耦：通过 `QtConcurrent::run + QFutureWatcher + 回调接口` 传递日志与进度。
- 团队协作体会【待补充】：
  - 建议写明每位成员的分工、沟通方式、遇到的协作问题与改进。
- 工程实践心得：
  - Git 提交粒度和信息的重要性；
  - 测试先行/同步设计测试的价值；
  - 将复杂功能拆分为小模块的必要性。

## 九、对本实验过程及方法、手段的改进建议（示例）

- 对课程与指导的建议【按实际体验补充】：
  - 建议在前几周增加一次针对 Git/GitHub 实战的短训练；
  - 可以提供一个简单参考项目，帮助同学理解评分点与期望工程质量；
  - 建议统一提供一个 GoogleTest 或其他测试框架的最小模板，降低配置成本。
- 对自身的反思与改进方向：
  - 更早规划测试与文档工作，而不是临近截止再集中补齐；
  - 对于安全敏感功能（加密），在起步阶段就查阅标准实现与安全注意事项。

---

# 需求分析说明书

> 本节对应模板中“需求分析说明书（10 分）”部分，结构按模板要求展开。编号参考模板，实际排版以 Markdown 为主。

## 1. 任务概述

### 1.1 引言

- 本文档用于描述“文件备份软件”的业务需求与功能需求。
- 目标读者：
  - 项目组成员；
  - 实验指导教师；
  - 后续维护/扩展本项目的开发者。
- 文档目的：
  - 给出清晰、一致、可检查的需求说明；
  - 为后续系统设计、编码实现和测试提供基础依据；
  - 作为验收与评分时的需求基线。

### 1.2 综合描述

#### 1.2.1 产品的状况

- 产品背景：
  - 面向本科生软件工程综合实验的教学项目；
  - 不以商业发布为目的，但要求工程实践较为完整；
  - 需要覆盖文件备份软件的核心功能和若干扩展需求。
- 与其它系统关系：
  - 当前版本为“单机备份工具”，未来可扩展为“网盘/云备份”。
  - 与操作系统文件系统交互（读取/写入/权限/时间戳/符号链接等）。
  - 与 Git/GitHub 集成，用于版本管理和团队协作。

#### 1.2.2 产品的功能（业务层面概览）

- 基本备份功能：
  - 选择源目录/文件，生成带 manifest 的备份；
  - 支持目录模式和 `.fbk` 打包模式；
  - 支持从备份中还原到指定目录。
- 数据验证：
  - 为每个文件计算哈希并记录；
  - 支持对备份进行完整性验证。
- 压缩和加密：
  - 提供多种压缩算法选项（Huffman/RLE/Zlib）；
  - 提供多种加密算法选项（XOR/RC4/简化 AES-256）；
  - 支持“不开启压缩/加密”的配置。
- 定时备份：
  - 支持每天/每周/每月定时备份；
  - 支持备份保留数量管理。
- 实时备份：
  - 监控指定目录，当检测到文件变化时自动触发备份。
- 图形界面：
  - 通过 Qt GUI 提供备份 / 恢复 / 实时备份 / 定时备份 4 个标签页；
  - 实时显示操作日志与进度条。

#### 1.2.3 用户特征

- 目标用户：
  - 需要备份个人文件的学生/教师；
  - 初学软件工程的学生，用于学习完整开发流程。
- 用户经验假设：
  - 有基本的文件系统操作经验（会创建文件夹、复制文件）；
  - 无需掌握命令行或编程知识。

#### 1.2.4 约束

- 开发约束：
  - 必须使用 C++ + Qt + CMake；
  - 必须使用 Git/GitHub 进行版本管理；
  - 需要在学期时间内完成。
- 运行约束：
  - 不依赖大型后端服务；应在本地单机上可运行；
  - 在受限权限环境下（如学校机房）仍需尽量正常工作。

#### 1.2.5 假设与依赖

- 假设：
  - 用户有足够磁盘空间存储备份；
  - 用户不会恶意篡改 manifest 文件；
  - 备份期间系统不会频繁重启或断电（否则需手工清理部分中间文件）。
- 依赖：
  - 操作系统稳定提供文件系统 API；
  - Qt 库、C++ 编译器正确安装；
  - 如需运行自动化测试，则需要网络下载 GoogleTest（或预先缓存）。

## 2. 业务需求

### 2.1 业务背景

- 现实场景：学生/老师经常需要备份课程资料、代码、论文等文件；手动复制容易遗漏、错误或覆盖不当。
- 业务目标：
  - 提供“一键备份”和“一键恢复”的简单操作；
  - 支持日常使用中常见的扩展需求（压缩、加密、定时/实时备份）。

### 2.2 业务流程（如何画业务流程图）

- 建议绘制一张业务流程图（可使用 draw.io、ProcessOn、Visio、StarUML 等）：
  - 典型流程 1：手动备份
    - 用户启动程序 → 选择源目录 → 选择目标目录 → 配置选项 → 执行备份 → 查看结果与日志。
  - 典型流程 2：恢复
    - 用户启动程序 → 选择备份文件/目录 → 选择恢复目标 → 输入密码（如有）→ 执行恢复 → 查看结果。
  - 典型流程 3：定时备份
    - 用户在设置页配置源/目标、频率和时间 → 启用定时备份 → 程序在后台定时触发备份 → 维护备份数量。
  - 典型流程 4：实时备份
    - 用户配置监控目录和备份位置 → 启用实时备份 → 文件变化 → 触发延迟计时 → 生成新的备份。
- 图中建议包含的元素：
  - 方框：业务活动（如“生成 manifest”、“写入备份包”）；
  - 菱形：决策（如“是否启用压缩？”、“是否加密？”）；
  - 圆角矩形：开始/结束节点；
  - 箭头：流程方向。
- 【待补充：将绘制好的流程图以 PNG/SVG 形式保存，并在 Word 版报告中插入。】

## 3. 功能需求

### 3.1 用例列表（示例）

- UC01：创建备份
- UC02：恢复备份
- UC03：验证备份
- UC04：配置定时备份计划
- UC05：执行定时备份
- UC06：开启实时备份监控
- UC07：查看备份/恢复日志

### 3.2 用例描述（示例：UC01）

- 用例编号：UC01
- 用例名称：创建备份
- 参与者：普通用户
- 前置条件：
  - 程序已启动；
  - 用户已选择源路径和目标路径。
- 基本流程：
  1. 用户在“备份”页选择源路径；
  2. 用户选择目标路径；
  3. 用户配置压缩/加密等选项；
  4. 用户点击“开始备份”；
  5. 系统执行备份，显示进度条和日志；
  6. 备份完成后显示成功信息和备份位置。
- 备用流程：
  - 3a. 若目标路径不可写：系统提示错误并要求重新选择。
  - 3b. 若启用加密但未填写密码：系统阻止执行并提示用户补充密码。

> 其它用例 UC02–UC07 可类比展开，建议在 Word 版中按模板逐条列出。

### 3.3 用例图（绘制建议）

- 建议使用 UML 工具绘制一张用例图：
  - Actor：用户；
  - Use Cases：UC01–UC07；
  - 将定时备份、实时备份作为扩展用例，使用 «include»/«extend» 关系表示；
  - 图中标注系统边界（“文件备份软件”）。
- 【待补充：将用例图导出为 PNG/SVG 并插入 Word 报告。】

## 4. 非功能需求

### 4.1 性能需求

- 备份性能：
  - 在普通教学 PC 上，对包含约 1 万个小文件或数 GB 数据的目录，备份应在可接受时间（例如数分钟）内完成；
  - 压缩与加密会增加时间开销，允许用户自主选择。
- 恢复性能：
  - 恢复时间与备份规模大致成正比，应不明显慢于备份过程。

### 4.2 安全性需求

- 提供基础级别的数据保护（防止随意浏览备份内容）：
  - 支持多种加密方式；
  - 若用户开启加密，则恢复和验证时必须输入密码。
- 安全声明：
  - 当前加密实现为教学用途，并非经过严格审计的工业级实现；
  - **不推荐**用于保护特别敏感的机密数据。

### 4.3 易用性需求

- GUI 需对普通用户友好，避免专业术语堆砌；
- 所有关键控件应有 Tooltip 提示；
- 错误信息应尽量明确，避免仅显示“出错”。

### 4.4 可维护性与扩展性

- 备份引擎与 UI 分离，通过 `BackupOptions`/`BackupResult` 等结构交互；
- 压缩/加密方法基于枚举和策略函数，可添加新算法而不影响现有调用；
- Manifest 格式中包含版本号和方法字段，便于向后兼容。

## 5. 项目规划

### 5.1 版本划分（基于 Git 提交记录）

结合 `git log --oneline --reverse` 可大致划分如下阶段：

- v0.1 初始仓库与规划
  - `4de6f0a Initial commit`
  - `8fb7bac Initial plan`
  - `7191749 上传课程资源`
  - `a0ae560 上传项目工作流初步规定`
- v0.2 需求分析完成
  - `ddf0ae2 初步完成需求分析`
- v0.3 基础 GUI 架构
  - `5d1cfee 基础Qt框架`
- v0.4 备份/恢复核心功能
  - `5cf7422 Add backup and restore functionality with manifest handling`
- v0.5 Huffman 压缩与恢复 UI 优化
  - `d4b6556 feat: add huffman compression and update restore picker`
- v0.6 maindev 合入主干
  - `0bc942f Merge pull request #2 from AntimoOfficial/maindev`
- v0.7 多算法 + 实时备份 + 用户引导 + 测试
  - `b63b129 支持更多加密、压缩算法；支持实时备份；完善用户引导`
  - `39c55ad 添加3个单元测试及1个集成测试`

### 5.2 项目总体规划（示例，可结合实际周次调整）

- 第 1–2 周：需求分析阶段
  - 阅读实验指导书和模板；
  - 明确功能范围与扩展目标；
  - 完成《需求分析说明书》初稿；
  - 产出：需求文档 v0.9。
- 第 3–4 周：系统总体设计
  - 确定系统架构与模块划分；
  - 设计备份格式与 manifest 结构；
  - 设计压缩/加密算法接口；
  - 完成《系统设计文档》初稿。
- 第 5–7 周：核心功能实现
  - 搭建 Qt GUI 框架；
  - 完成基础备份/恢复/验证流程；
  - 实现 Huffman 压缩及 XOR 加密；
  - 初步手工测试。
- 第 8–10 周：扩展功能与测试
  - 实现 RLE/Zlib 压缩、RC4/简化 AES 加密；
  - 实现定时备份和实时备份；
  - 编写 GoogleTest 单元测试与集成测试；
  - 完善错误处理和日志。
- 第 11–12 周：文档完善与演示准备
  - 完成实验报告、需求/设计/测试文档；
  - 整理用户使用指南与 README；
  - 进行最终演示和答辩准备。

### 5.3 里程碑计划（建议）

- M1：需求评审通过；
- M2：系统设计评审通过；
- M3：实现基础备份/恢复并可成功运行；
- M4：实现至少一种压缩和一种加密，完成端到端测试；
- M5：实现实时/定时备份；完成测试报告；
- M6：全部文档完成，实验报告提交。

## 6. 附录

### 6.1 术语表（示例）

- Manifest：备份清单文件，记录每个备份文件的元数据、哈希等。
- `.fbk`：自定义备份包格式文件。
- Huffman/RLE/Zlib：三种无损压缩算法。
- XOR/RC4/AES-256：三种加密方法（本项目中的 AES-256 为教学简化版）。

### 6.2 参考资料

- 实验指导书 PDF（仓库 `资源/` 目录）；
- Qt 官方文档；
- C++ 标准库文档；
- 课程教材《软件工程》、《数据结构与算法》等。

---

# 系统设计文档

> 本节对应模板中“系统设计文档”部分，重点描述架构设计、模块划分、数据设计和接口设计。图形化内容以文字说明+绘图建议给出。

## 1. 引言

- 说明文档目的：为开发和维护人员提供系统的整体和详细设计说明；
- 说明应用范围：文件备份软件当前版本及后续小版本；
- 引用文档：需求分析说明书、实验指导书、用户使用指南。

## 2. 总体设计

### 2.1 设计目标

- 功能正确：保证备份/恢复/验证流程在正常和常见异常场景下工作可靠；
- 易用：面向非专业用户，界面清晰、操作简单；
- 可扩展：便于增加新的压缩/加密算法、网络备份等扩展功能；
- 可测试：核心逻辑与 UI 解耦，便于自动化测试。

### 2.2 系统架构

- 分层结构：
  - 表现层（UI）：`MainWindow`（`src/main.cpp`），使用 Qt Widgets 实现 4 个标签页；
  - 业务逻辑层（备份引擎）：`BackupEngine`（`include/BackupEngine.h` / `src/BackupEngine.cpp`）；
  - 数据模型层：`BackupTypes`（`include/BackupTypes.h` / `src/BackupTypes.cpp`）定义 manifest、文件记录、选项和结果；
  - 测试层：`tests/` 中的 GoogleTest 用例。
- 建议绘制架构图：
  - 使用 UML 组件图或分层图，展示模块之间依赖关系；
  - 标明 UI 仅通过 `BackupOptions`/`BackupResult` 结构与引擎交互。

### 2.3 运行环境

- OS、依赖库与构建工具，参考前文“实验器材”部分；
- 部署方式：编译生成单个可执行文件 `file-backup-software`，无需额外服务端。

## 3. 模块设计

### 3.1 备份引擎模块（BackupEngine）

- 主要职责：
  - 扫描源目录，生成 `FileRecord` 列表；
  - 处理压缩/加密，将数据写入目录或 `.fbk` 包；
  - 写入/读取 manifest；
  - 提供 `performBackup`、`restore`、`verify` 三个主入口；
  - 维护保留策略、处理特殊文件和元数据。
- 关键接口：
  - `BackupResult performBackup(const BackupOptions&, const BackupEngineCallbacks&);`
  - `RestoreResult restore(const RestoreOptions&, const BackupEngineCallbacks&);`
  - `VerifyResult verify(const VerifyOptions&, const BackupEngineCallbacks&);`

### 3.2 压缩/加密模块

- 压缩策略：
  - 通过 `CompressionMethod` 枚举选择具体算法；
  - `compressData(data, method)` / `decompressData(data, method, expectedSize, ok)`；
  - Huffman/RLE/Zlib 的实现各自独立，便于扩展。
- 加密策略：
  - 通过 `EncryptionMethod` 枚举选择算法；
  - `encrypt(data, password, method)` / `decrypt(data, password, method, ok)`。
- 建议绘制类图：
  - 展示 `BackupEngine` 与压缩/加密辅助函数之间的依赖；
  - 为未来重构为“策略类”预留空间。

### 3.3 定时备份模块

- UI 部分：定时备份标签页中的路径设置、选项设置、频率/时间/保留数量配置；
- 逻辑部分：
  - 使用 `QTimer` 每分钟检查是否到达下一次执行时间；
  - 根据配置构造 `BackupOptions`，调用 `runBackup(options, scheduled=true)`；
  - 调用 `enforceRetention` 根据保留数量清理旧备份。

### 3.4 实时备份模块

- 使用 `QFileSystemWatcher` 监控目录变化，配合 `QTimer` 做防抖；
- 当检测到文件变化后，在设定延迟后触发一次备份；
- 同样使用 `BackupOptions` 与引擎交互，沿用现有备份/验证逻辑。

### 3.5 UI 模块（MainWindow）

- 负责：
  - 创建和布局 4 个标签页；
  - 收集用户输入并构造 `BackupOptions`/`RestoreOptions`/`VerifyOptions`；
  - 通过 `QtConcurrent::run` 调用 `BackupEngine`，并通过 `BackupEngineCallbacks` 更新日志与进度；
  - 维护定时和实时备份的状态。

## 4. 数据设计

### 4.1 数据结构（核心结构体）

- `FileRecord`：对应一个文件或目录的元数据。
  - 字段：`relativePath`、`kind`、`size`、`storedSize`、`hash`、`storedHash`、`permissions`、`ownerId`、`groupId`、时间戳、`symlinkTarget`、`specialDevice` 等。
- `BackupManifest`：描述一次完整备份。
  - 字段：`backupId`、`rootName`、`createdAt`、`sourcePath`、`storageMode`、`compressed`、`encrypted`、`compressionMethod`、`encryptionMethod`、文件列表等。
- `BackupOptions` / `RestoreOptions` / `VerifyOptions` / 结果结构体：用于函数调用与结果返回。

### 4.2 数据持久化格式（Manifest 与包格式）

- Manifest：
  - JSON 文件 `manifest.json`，可人类阅读；
  - 使用 `manifestToJson` / `manifestFromJson` 进行序列化/反序列化；
  - 兼容旧版本缺少压缩/加密方法字段的情况。
- `.fbk` 包格式：
  - 包头：`"FBS1"` 魔数 + 版本号 + manifest 偏移和长度；
  - 中间：连续的数据块（可能压缩/加密），每块位置由 `FileRecord::dataOffset` 描述；
  - 结尾：manifest JSON 原文。

### 4.3 数据流（绘制建议）

- 建议绘制 0/1 级数据流图（DFD）：
  - 外部实体：用户、文件系统；
  - 处理：备份引擎、压缩/加密模块、定时/实时调度器；
  - 数据存储：备份目录或 `.fbk` 文件；
  - 数据流：源文件数据流向备份引擎，再流向备份目标；恢复时反向。
- 【待补充：根据实际实现绘制 DFD，并在 Word 报告中插入。】

## 5. 接口设计

- UI 与引擎之间：
  - 仅通过 `BackupOptions` / `RestoreOptions` / `VerifyOptions` 结构交互；
  - UI 不直接操作 manifest 或具体文件。
- 引擎与文件系统之间：
  - 使用 `std::filesystem` + Qt 的 `QFile` / `QDir`；
  - 使用 POSIX API 处理 Unix 特有的元数据和特殊文件。
- 测试接口：
  - 通过 `backup_core` 静态库，测试代码可以直接调用 `BackupEngine` 和 `BackupTypes`。

## 6. 错误处理与日志设计

- 错误传播：
  - 引擎函数通过返回结构体中的 `success` 和 `error` 字段报告错误。
- 日志：
  - 通过 `BackupEngineCallbacks::log` 回调向 UI 输出人类可读的日志；
  - 建议后续增加“写入日志文件”功能，便于排查长期问题。

## 7. 安全设计

- 当前加密实现用于教学示范：
  - XOR 和 RC4 实现清晰、易懂；
  - 简化 AES-256 模拟了 S-box、轮变换和填充，但不等同于标准 AES。
- 风险提示：
  - 不宜用于强安全需求场景；
  - 建议后续如需增强安全性，应引入经过验证的加密库（如 OpenSSL/QtCrypto 等），并遵循密码学最佳实践。

## 8. 部署与扩展设计

- 部署方式：
  - 通过 CMake 生成单一可执行文件；
  - 若需要用户友好安装，可使用 Qt Installer 或平台原生打包工具。
- 未来扩展方向：
  - 网络备份、增量备份、版本管理；
  - 后端数据库（此时可真正使用模板中“基表设计/视图设计/索引设计”的表格格式来设计关系模式）；
  - 插件式压缩/加密算法扩展机制。

## 9. 附录（UML 图绘制建议）

- 用例图：见需求文档；
- 类图：展示 `BackupEngine`、`BackupTypes`、`MainWindow`、测试辅助类之间关系；
- 时序图：展示“创建备份”过程中 UI → 引擎 → 文件系统 → UI 的调用顺序；
- 部署图：展示可执行文件在单机环境中的部署结构。

---

# 软件测试报告

> 本节对应模板中“软件测试报告（20 分）”部分，结合仓库 `tests/` 目录中的 GoogleTest 用例描述。

## 1. 引言

- 测试目的：
  - 尽可能发现软件中的缺陷，提高质量；
  - 验证需求分析与系统设计中规定的功能是否被正确实现；
  - 为实验验收提供客观依据。
- 测试范围：
  - 备份/恢复/验证核心流程；
  - 压缩/加密算法的正确性与兼容性；
  - 定时/实时备份的关键逻辑（主要通过集成测试和手工测试）。

## 2. 测试环境

- 硬件：普通教学 PC；
- 软件：
  - 操作系统：当前以 macOS 环境为主验证，其它平台需补充测试；
  - 编译工具链：CMake + C++17 编译器；
  - Qt 6.x；
  - GoogleTest 1.14（通过 CMake FetchContent 引入）。
- 测试构建与运行：
  - 在工程根目录执行：
    - `cmake -B build`
    - `cmake --build build`
  - 单独为测试构建：
    - `cd tests`
    - 运行 `run_tests.sh`（脚本中会调用 CMake/ctest，需网络访问以拉取 GTest；在网络受限环境下需预先下载或本地缓存）。

## 3. 功能测试

### 3.1 单元测试（tests/test_*.cpp）

- `test_backup_types.cpp`：
  - 测试 `BackupTypes` 中枚举转换函数和 JSON 序列化/反序列化；
  - 确保 manifest 写入/读取不会丢失关键字段（尤其是压缩/加密方法枚举）。
- `test_compression.cpp`：
  - 对 Huffman/RLE/Zlib 等压缩算法进行“压缩→解压→比对”的往返测试；
  - 覆盖空数据、短文本、重复数据、大数据等场景；
  - 验证 `expectedSize` 参数检查逻辑。
- `test_encryption.cpp`：
  - 对 XOR/RC4/AES-256 加密算法进行“加密→解密→比对”测试；
  - 覆盖不同密码、空密码（应失败）等情况；
  - 检查错误处理（如魔数不匹配时的失败）。
- `test_utils.cpp`：
  - 提供测试辅助函数，如创建临时目录/文件、比较目录内容等。

### 3.2 集成测试（tests/test_backup_restore.cpp）

- 测试目标：
  - 验证整个备份-恢复链路在不同配置下的正确性。
- 典型场景：
  - 仅备份单个文件（不压缩、不加密），检查恢复结果与原文件一致；
  - 备份包含子目录、符号链接、特殊文件的目录；
  - 使用不同压缩/加密组合（例如 Zlib + AES）进行 round-trip 测试；
  - 目录模式与 `.fbk` 包模式的对比。

### 3.3 手工 GUI 测试

- 按用户使用指南中的场景一至六进行手工操作：
  - 简单备份与恢复；
  - 加密备份与恢复（输入正确/错误密码）；
  - 打包/解包；
  - 定时备份（调整系统时间或等待定时器触发）；
  - 实时备份（在监控目录中新增/修改/删除文件，观察自动备份行为）。
- 建议按模板中的用例表格式记录测试用例编号、输入、操作步骤、预期结果、实际结果和测试人员。

## 4. 代码测试（可选，建议）

- 代码规范检查：
  - 检查命名风格、注释质量、函数长度等；
  - 建议使用 clang-tidy 或 IDE 提供的静态分析，当前仓库尚未集成自动化静态分析配置。
- 潜在错误检查：
  - 内存泄漏、未初始化变量、越界访问等；
  - 可以结合 AddressSanitizer/Valgrind 等工具进行深度检查【需额外配置】。

## 5. 性能测试（可选，建议）

- 建议的性能测试场景：
  - 备份大量小文件：例如 1 万个 4KB 文件，测量备份和恢复所需时间；
  - 备份大文件：例如 1GB 文件，在不同压缩算法下的时间和压缩比；
  - 实时备份压力：在监控目录中频繁创建/修改文件，观察响应时间和系统资源占用。
- 当前仓库中尚未包含性能测试脚本和数据集：
  - 【待补充：根据课程时间和机器性能编写简单脚本，记录性能数据并附在报告中。】

## 6. 健壮性测试（可选，建议）

- 人为干扰场景：
  - 在备份过程中关闭程序或拔掉磁盘；
  - 使用不可读/不可写目录作为源/目标；
  - 提供损坏的 `.fbk` 文件或被篡改的 manifest。
- 检查点：
  - 程序能否给出明确错误提示；
  - 是否发生崩溃或未捕获异常；
  - 是否留下不可恢复的中间状态（例如部分写入的文件）。
- 当前实现主要通过异常处理和错误字符串进行反馈，尚未专门针对健壮性做系统测试：
  - 【待补充：设计若干健壮性测试用例，并在后续版本中实现自动化测试。】

## 7. 测试结果分析与改进建议

- 从现有单元测试和集成测试来看：
  - 核心数据结构与压缩/加密算法在常规场景下表现正确；
  - 备份-恢复流程在多种配置下能完成 round-trip；
  - GUI 手工测试覆盖了主要使用路径。
- 仍需改进之处：
  - 增加对极端输入（超大文件、极多小文件）的测试；
  - 增强错误路径测试，如故意构造损坏的备份文件；
  - 引入性能与健壮性自动化测试，用于回归验证；
  - 为网络受限环境准备离线的 GoogleTest 包，保证测试在教学环境中可重复执行。

---

> 以上内容可直接作为多文档的“汇总版实验报告”。如需提交 Word/PDF 版本，可将本文件各章节复制到对应文档中，并根据模板要求插入 UML 图、数据流图、界面截图和性能测试表格。*** End Patch*** */}
assistant to=functions.apply_patchերազմassistant to=functions.apply_patch:variables નજીક to=functions.apply_patch ***!
